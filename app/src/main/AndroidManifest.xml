<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.wgw.table.demo"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.wgw.table.kotlin.demo.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name="com.wgw.table.kotlin.demo.TableActivity"
            android:configChanges="screenSize|keyboard|orientation"/>
        <activity
            android:name="com.wgw.table.kotlin.demo.SurfaceTableActivity"
            android:configChanges="screenSize|keyboard|orientation"/>
    </application>

</manifest>